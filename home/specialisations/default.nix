_: {
  # TODO builtins readdir like wrapper-manager
  # TODO specialisation sync with os script
  # but is sync possible?
  # just make a specialisation fzf switcher thing?
  imports = [
    ./specialisations.nix
    ./no-specialisation.nix
  ];
}
